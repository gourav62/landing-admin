{
  "name": "landing-admin-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "seed": "node seed.js",
     "install-client": "cd frontend && npm install",
    "build-client": "cd frontend && npm run build",
    "postinstall": "npm run install-client",
    "build": "npm run build-client",
    "start": "cd backend && npm install --production && node index.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "body-parser": "^1.20.0",
    "cloudinary": "^2.8.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.0.0",
    "multer": "^2.0.2",
    "react-router-dom": "^7.9.6",
    "streamifier": "^0.1.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
